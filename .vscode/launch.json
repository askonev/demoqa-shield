{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "pytest",
            "type": "debugpy",
            "request": "launch",
            "module": "pytest", // Запускаем pytest как модуль
            "args": [
                "${file}" // Передаем текущий файл как аргумент
            ],
            "console": "integratedTerminal",
            "justMyCode": false // Позволяет отладчику заходить в код сторонних библиотек
        }
    ]
}